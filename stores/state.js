'use strict';

export const state = {
    user: null,

    // an image saved to retrieve it later
    image: null,
    // the filter on the image
    selectedFilter: null,
    // a caption for the image
    caption: null,

    // history stack
    history: [],
};